# OS
.DS_Store

# Local Claude tooling (keep local settings out of VCS)
.claude/settings.local.json
.claude/launch.json

# Brainstorm scratch (mockups served to the browser during design sessions)
.superpowers/

# Node (sub-projects carry their own lockfiles; nothing node-y at repo root)
/node_modules/
/package-lock.json

# Env / secrets
.env
.env.local
.env.*.local

# Local compose overrides (per-machine host-port remaps etc.)
docker-compose.override.yml

# Logs
*.log
