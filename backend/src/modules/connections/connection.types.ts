/** Supported source systems (BC-0). The Connection row's `sourceSystem` column. */
export type SourceSystem =
  | 'jira'
  | 'github'
  | 'gitlab'
  | 'azure-devops'
  | 'sonarqube'
  | 'jenkins'
  | 'github-actions';
