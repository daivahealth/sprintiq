import { Injectable } from '@nestjs/common';

/**
 * BC-6 Build, Release & CI/CD. Pipelines, builds, deployments, environments,
 * releases — the basis for DORA reliability metrics. Consumes ci.* domain
 * events from collectors. (Scaffold stub.)
 */
@Injectable()
export class CiService {}
