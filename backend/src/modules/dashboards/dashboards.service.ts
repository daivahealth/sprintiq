import { Injectable } from '@nestjs/common';

/**
 * BC-13 Dashboards & Reporting. Read-optimized BFF for per-persona dashboards,
 * widgets, drill-downs, filters, exports. Surfaces data freshness / linkage
 * coverage / metric health alongside every number. (Scaffold stub.)
 */
@Injectable()
export class DashboardsService {}
